// As we talked in the Odd/Pair value example, in this case we have a similar situation.
// To define the positive/negative condition, we must find out a proper way (If/Else) to respond.


#include <stdio.h>
#include <stdlib.h>

int main(){

    int numero;

    printf("Ingrese el numero:\n");
    scanf("%d", &numero);

    if (numero > 0){
        printf("El numero es mayor que cero y por ende positivo\n");

    }else if (numero = 0){
        printf("El numero es igual a Cero\n");

    }else if (numero < 0){
        printf("El numero es negativo\n");
    }

    return 0;
}
