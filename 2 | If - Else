// As we talked in the Odd/Pair value example, in this case we have a similar situation.
// To define the positive/negative condition, we must find out a proper way (If/Else) to respond.


#include <stdio.h>
#include <stdlib.h>

int main(){

    int number;

    printf("Give me some number:\n");
    scanf("%d", &number);

    if (number > 0){
        printf("The number looks bigger than Zero so the number looks positive\n");

    }else if (number = 0){
        printf("The number looks similar than Zero\n");

    }else if (number < 0){
        printf("The number looks negative!\n");
    }

    return 0;
}
