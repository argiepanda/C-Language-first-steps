// In this case you have a singular problem or issue. You have to talk with the external/internal user.
// The fundamental issue or resolution is the definition of the value.
// You have to find the way (using If-Else resource) to define if the value is odd or pair.

#include <stdio.h>
#include <stdlib.h>

int main(){

    int number;

    printf("Give me a number please:\n");
    scanf("%d", &number);

    if (number % 2 == 0){
        printf("Pair number!\n");

    }else{
        printf("Odd number!\n");
    }

    return 0;
}
